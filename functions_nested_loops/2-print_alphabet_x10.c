#include "main.h"
/**
 * print_alphabet_x10 - 10 dfe elifbani cap ele 
 * @h: herfleri temsil edecek. yazmaq lazimdir goresen
 * @i: integer sayi
 */
void print_alphabet_x10(void)
{
	char h = 'a';
	int i;
	for (i = 0; i < 10; i++)
	{
		while (h <= 'z')
		{
			_putchar(h);
			h++;
		{
	_putchar ('\n');

	}
}
