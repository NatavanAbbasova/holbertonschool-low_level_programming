#include "main.h"
/**
 * _abs - o vaxt vaxtim olmurdu bunlari elemeye
 * gelib baxinca cox asan gelirler
 * @n: men
 * Return: returnnn
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
