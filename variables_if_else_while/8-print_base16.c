#include <stdio.h>
/**
 * main - men de bunlari ciddi sanirdim
 *
 * Return: seni de :'(
 */
int main(void)
{
	char ch;

	for (ch = '0'; ch <= '9'; c++)
	{
		putchar(ch);
	}

	for (ch = 'z'; ch >= 'a'; ch--)
		putchar(ch);
	putchar('\n');

	return (0);
}
